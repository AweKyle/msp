
html, body 
{
	margin: 0;
	padding: 0;
	height: 100%;
}

#container
{
	min-height: 100%;
	height: 100%;
	position: relative;
}

#header
{
	background: #DCDCDC;
	padding: 10px;
}

#body
{
	padding: 10px;
	padding-bottom: 20px;	/* высота футера */
}

#footer
{
	position: absolute;
	bottom: 0;
	width: 100%;
	height: 20px;	/* высота футера */
	background: #6cf;
}

li {
	padding: 3px;
}

/*
 * Убираем двойную рамку в таблицах
 * */
table {
	border-collapse: collapse;
}

/*
 * Задаем фон в элементах th
 * */
th {
	background-color: #DCDCDC;
}

/*
#div_header {
	overflow: hidden !important;
	width: 100%;
}
*/

#user_name, #div_out {
	background-color: #DCDCDC;
	width: 100%;
	height: 17px;
	text-align: right;
	margin-top: 0;
	margin: 0;
	left: 0;
	right: 0;
	padding: 0;
	margin-right: 0;
}

#user_name, #div_out {
	display: inline-block;
}

/*
 * Скрываем элемент radio
 * */
.rad
{
	display: none;
}

/*
 * Задаем стиль кнопкам submit
 * */
.sbmt {
  padding: 6px 10px;
  -webkit-border-radius: 2px 2px;
  border: solid 1px rgb(153, 153, 153);
  background: -webkit-gradient(linear, 0% 0%, 0% 100%, from(rgb(255, 255, 255)), to(rgb(221, 221, 221)));
  color: #333;
  text-decoration: none;
  cursor: pointer;
  display: inline-block;
  text-align: center;
  text-shadow: 0px 1px 1px rgba(255,255,255,1);
  line-height: 1;
}

/*
 * задаем стиль для выпадающих списков (select)
 * */
.course, #semester_list, #chair_list, .speciality_list, .lists_for_subj {
    width: 180px;
    height: 23px;
    padding: 5px 0 0 8px;
    color: #394022;
    font-size: 13px;
    overflow: hidden;
}

/*
 * вносим поправки к основному стилю select'ов для списка специализаций 
 * при добавлении нового предмета
 * */
.lists_for_subj {
    width: 450px;
    height: 120px;
}

/*
 * вносим поправки к основному стилю select'ов для списка семестров
 * */
#semester_list {
    height: 120px;
}

/*
 * вносим поправки к основному стилю select'ов для списка курсов
 * */
.course {
	width: 140px;
}

/*
 * Задаем стиль для текстовых полей
 * */
.textbox, #stud_count, .add_struct, .stud_info, .auth, #c_year {
    border:1px solid #9E9E9E;
    color: #000000;
    width: 240px;
    padding: 3px;
    margin-top: 2px;
    margin-bottom: 2px;
    font-size: 13px;
}

/*
 * вносим поправки к основному стилю текстовых полей 
 * для числа студентов
 * */
#stud_count {
	width: 140px;
}

/*
 * вносим поправки к основному стилю текстовых полей 
 * для названия предметов
 * */
#subj_name {
	height: 120px;
	resize: none; 
	padding: 0;
    margin-top: 0;
    margin-bottom: 0;
}
/*
 * стиль для поля "учебный год"
 */
#c_year{
	width: 50px;
}

#authorization {
	left: 50%;
	margin-left: -500px;
	position: absolute;
	width: 1000px;
}

#reg_div {
	left: 50%;
	margin-left: -500px;
	position: absolute;
	width: 500px;
	background: #6cf;
}

#l_div {
	background: #6cf;
	float: left;
}

#r_div {
	float: center;
}